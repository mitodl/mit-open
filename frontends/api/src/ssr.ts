export {
  isServer,
  Hydrate,
  QueryClient,
  QueryClientProvider,
  dehydrate,
  useQueries,
} from "@tanstack/react-query"

export type { UseQueryResult, UseQueryOptions } from "@tanstack/react-query"
