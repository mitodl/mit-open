// import { ExpandedLearningResourceDisplay } from "./ExpandedLearningResourceDisplay"

describe("ExpandedLearningResourceDisplay", () => {
  it.each([
    // all the types
    1, 2, 3,
  ])("Renders title and resource type", () => {
    // render the component
    // check that the title and resource type are rendered
    // maybe check image?
  })

  it("Renders video for videos", () => {
    // render the component with a video
    // check that the video is rendered
  })
})
