import { Canvas, Meta } from "@storybook/blocks";

import * as ButtonStories from "./Button.stories";

<Meta of={ButtonStories} />

# Button

Use prop `variant="primary" | "secondary" | "tertiary" | "text"` to set the button variant:

<Canvas of={ButtonStories.VariantStory} />

Use `size="small" | "medium" | "large"` to set the button size:

<Canvas of={ButtonStories.SizeStory} />

Use `disabled` to disable the button:

<Canvas of={ButtonStories.DisabledStory} />

Use `edge="circular" | "rounded"` to set the button edge:

<Canvas of={ButtonStories.EdgeStory} />

Use `startIcon` and `endIcon` to add icons to the button. The icons are expected
to be from [`@mui/icons-material`](https://mui.com/material-ui/material-icons/):

<Canvas of={ButtonStories.WithIconStory} />

## Links

To render a link styled as a Buttons, use `ButtonLink`. The underlying component will be a `react-router` Link.
<Canvas of={ButtonStories.LinkStory} />

## Icon-Only Buttons

Use `<ActionButton />` to render an icon-only button:

<Canvas of={ButtonStories.IconOnlyStory} />

## More Examples

<Canvas of={ButtonStories.ButtonsShowcase} />

<Canvas of={ButtonStories.ActionButtonsShowcase} />
