"""Testimonials app setup."""

from django.apps import AppConfig


class TestimonialsConfig(AppConfig):
    """Configuration settings for the app."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "testimonials"
